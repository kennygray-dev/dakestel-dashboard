{
  "name": "dakestel-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Sales dashboard: Express/MongoDB API plus React/Vite frontend",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix dakestel-dashboard-frontend run dev",
    "build": "npm run build:api && npm run build:web",
    "build:api": "npm --prefix backend run build",
    "build:web": "npm --prefix dakestel-dashboard-frontend run build",
    "start": "npm --prefix backend run start",
    "install:all": "npm install && npm --prefix backend install && npm --prefix dakestel-dashboard-frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
